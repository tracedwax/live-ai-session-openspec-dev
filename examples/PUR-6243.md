# PUR-6243: Cloud UI PCP & PDP: URL voucher code breaks page load after modal dismissal

**Type:** Bug · **Project:** Purchase · **Status:** Done
**Use as:** the shape to copy, clean Given/When/Then + test mapping.

## Description
On Cloud UI PCP & PDP, passing a voucher code via URL stops loading the root URL after a successful voucher modal, and sends the user to a "page not found" if the modal is dismissed.

## Acceptance Criteria

**AC 1, Non-fenced Cloud PDP or PCP page + valid discount voucher code**
- GIVEN a user navigates to a non-fenced Cloud PDP or PCP URL with a valid discount voucher code appended
- WHEN the success modal appears and the user dismisses it (via X or clicking outside)
- THEN the Cloud PDP or PCP page loads successfully with no interruption or stall

**AC 2, Non-fenced Cloud PDP or PCP page + invalid voucher code**
- GIVEN a user navigates to a non-fenced Cloud PDP or PCP URL with an invalid voucher code appended
- WHEN the error modal appears and the user dismisses it (via X, Cancel, or clicking outside)
- THEN the page loads successfully, the "Page not found" page must NOT load

## Testing Notes (regression)

**Fenced voucher page + non-matching code**
- GIVEN a fenced voucher product page URL with an invalid or non-granting code
- WHEN the modal is dismissed
- THEN the PUI "Page not found" page loads

**Fenced voucher page + valid matching product voucher code (must not regress)**
- GIVEN a fenced voucher page with a valid matching product voucher code
- WHEN the modal is handled
- THEN the user is redirected to the fenced product page with the voucher loaded

## AC → Test Case → Evidence (the QA bit to copy)

| Test feature | AC | Test case | Evidence |
|---|---|---|---|
| Valid URL voucher auto-applies | AC1 | QUAL-5013, QUAL-4736 | _(video evidence on ticket)_ |
| Invalid URL voucher shows error | AC2 | QUAL-5014 | _(video evidence on ticket)_ |
| Fenced page + non-matching code | AC3 | QUAL-5015, QUAL-2591 | _(video evidence on ticket)_ |

> QUAL test-case bodies aren't in this export, treat the IDs as placeholders.
